# Option 1 (recommended for deployment):
# Single SQLAlchemy connection string
DATABASE_URL=postgresql+psycopg2://username:password@host:5432/database_name

# Option 2 (local development):
DB_USER=your_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=opensource_pipeline

# Optional GitHub token for collector script
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
